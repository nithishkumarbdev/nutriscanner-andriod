# ML Kit and Firebase ship their own consumer ProGuard rules.
# Nothing custom needed yet; add rules here if release builds start stripping
# something the reflection-based Firestore model mapping needs.
